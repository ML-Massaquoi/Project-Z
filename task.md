# Project Z — Premium Dark Theme Restoration Tasks

- [ ] Restore Core Dark Tokens
  - [ ] Modify `design-tokens.css` with dark navy-gray workspace background (`#0B0F17`), deep slate card surfaces (`#131823`), dark input background (`#0D111A`), and off-white/light gray texts (`#F8FAFC`/`#E2E8F0`).
  - [ ] Modify `tokens.ts` to sync JS colors and Recharts `chartTheme` to dark mode.
  - [ ] Modify `index.css` to update Tailwind theme colors, set `html` `color-scheme` to `dark`, and restore dark `.glass` utility styles.
- [ ] Component & Page Sync
  - [ ] Update `Backups.tsx` statusConfig with dark-theme badges.
  - [ ] Update `Shifts.tsx` summary metrics card accent colors for dark card backgrounds.
  - [ ] Update `SystemHealth.tsx` live-observability badge styling.
- [ ] Verification
  - [ ] Review UI contrast and element boundary legibility in the dark theme.
